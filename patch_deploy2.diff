--- .github/workflows/deploy.yml
+++ .github/workflows/deploy.yml
@@ -52,9 +52,9 @@
           cache: "pnpm"
       - run: pnpm install
-      - run: pnpm dlx vercel pull --yes --environment=production --token="$VERCEL_TOKEN"
-      - run: pnpm dlx vercel build --prod --token="$VERCEL_TOKEN"
-      - run: pnpm dlx vercel deploy --prebuilt --prod --token="$VERCEL_TOKEN"
+      - run: pnpm dlx vercel pull --yes --environment=production --token=${{ env.VERCEL_TOKEN }}
+      - run: pnpm dlx vercel build --prod --token=${{ env.VERCEL_TOKEN }}
+      - run: pnpm dlx vercel deploy --prebuilt --prod --token=${{ env.VERCEL_TOKEN }}

   deploy-preview:
@@ -95,8 +95,8 @@
       - name: Pull Vercel Environment Information
         if: ${{ env.VERCEL_TOKEN != '' }}
-        run: pnpm dlx vercel pull --yes --environment=preview --token="$VERCEL_TOKEN"
+        run: pnpm dlx vercel pull --yes --environment=preview --token=${{ env.VERCEL_TOKEN }}
       - name: Build Project Artifacts
         if: ${{ env.VERCEL_TOKEN != '' }}
-        run: pnpm dlx vercel build --token="$VERCEL_TOKEN"
+        run: pnpm dlx vercel build --token=${{ env.VERCEL_TOKEN }}
       - name: Deploy Project Artifacts to Vercel
         if: ${{ env.VERCEL_TOKEN != '' }}
-        run: pnpm dlx vercel deploy --prebuilt --token="$VERCEL_TOKEN"
+        run: pnpm dlx vercel deploy --prebuilt --token=${{ env.VERCEL_TOKEN }}
