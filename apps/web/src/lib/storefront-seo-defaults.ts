/** Generic category word used when a product has no merchant-specific category label. */
export const DEFAULT_STOREFRONT_SEO_CATEGORY = 'products' as const;

/** Fallback store name for tenant metadata when merchant branding is unavailable. */
export const DEFAULT_STORE_NAME = 'Baci Store' as const;
