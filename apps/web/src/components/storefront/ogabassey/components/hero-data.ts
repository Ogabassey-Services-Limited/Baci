import { OGABASSEY_HERO_MOBILE_LCP_FALLBACK_SRC } from '@/config/ogabassey-hero-assets';

// The hero is now driven by live launch products (see Hero / hero-desktop-grid
// / hero-mobile-carousel). The old hardcoded promo slides (iPhone / Titanium /
// MacBook / PS5) were retired with the product-driven hero. The one value still
// needed is the small themed fallback for the mobile LCP image's non-priority
// <source> decode path in MobileLcpHeroImage.
export const HERO_MOBILE_LCP_FALLBACK_SRC =
  OGABASSEY_HERO_MOBILE_LCP_FALLBACK_SRC;
