/**
 * Deprecated compatibility alias for older mobile app builds.
 *
 * Keep this route until the mobile admin app has fully migrated to
 * /api/paystack/resolve in production.
 */
export { POST } from '@/app/api/paystack/resolve/route';
