/**
 * Shared identity draft used to prefill and sync the NIN and BVN forms.
 */
export interface VerificationIdentityDraft {
  dateOfBirth: string;
  firstName: string;
  lastName: string;
  mobileNo: string;
}
