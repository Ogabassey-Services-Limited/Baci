/**
 * App metadata constants
 * Single source of truth for version strings displayed in the UI.
 */

const APP_VERSION = '2.0.1';
const APP_DISPLAY_NAME = 'Baci Admin';
export const APP_VERSION_LABEL = `${APP_DISPLAY_NAME} v${APP_VERSION}`;
