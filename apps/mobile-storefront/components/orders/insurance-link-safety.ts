// MyCover hosted-flow URL validation lives in @baci/shared (used by web too).
// Re-exported here so existing storefront imports keep working.
export {
  normalizeInsuranceCertificateUrl,
  normalizeInsuranceFlowUrl,
} from '@baci/shared/insurance';
