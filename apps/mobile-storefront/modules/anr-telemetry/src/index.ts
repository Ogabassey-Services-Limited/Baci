/**
 * The app-facing wrapper lives in `lib/anr-telemetry.ts` so web and Jest can
 * use the same no-op fallback. This package entry point exists for Expo's
 * local-module autolinker.
 */
export {};
