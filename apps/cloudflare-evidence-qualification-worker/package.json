{
  "name": "@baci/cloudflare-evidence-qualification-worker",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "test": "vitest run --config ../../packages/shared/vitest.config.ts",
    "typecheck": "mkdir -p .qualification-dist/types && wrangler types .qualification-dist/types/version-a.d.ts --config wrangler.version-a.jsonc && wrangler types .qualification-dist/types/version-b.d.ts --config wrangler.version-b.jsonc && tsc -p tsconfig.json && tsc -p tsconfig.version-b.json"
  },
  "devDependencies": {
    "vitest": "^4.1.9",
    "wrangler": "4.115.0"
  }
}
