YYYY-MM-DD — Storefront SEO Optimization
Learning: Raw `<script type="application/ld+json">` tags and manual stringification are risky and non-compliant with Baci's AGENTS.md directives.
Action: Always use the `<JsonLd>` component and cast to `JsonLdData` for structured data on public storefront pages.
Source: https://schema.org/LocalBusiness / https://schema.org/WebSite
