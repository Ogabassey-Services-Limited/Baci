## YYYY-MM-DD - Added accessibilityRole to modal buttons
**Learning:** Found that some buttons in `OrderItemDetailModal.tsx` were missing the `accessibilityRole` attribute.
**Action:** Always ensure that interactive elements like buttons have the appropriate `accessibilityRole` attribute for screen reader users.
