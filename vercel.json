{
  "buildCommand": "cd baci-web-platform && npm run build",
  "outputDirectory": "baci-web-platform/.next",
  "installCommand": "npm install && cd baci-web-platform && npm install",
  "framework": "nextjs",
  "crons": [
    {
      "path": "/api/ai-jobs/worker",
      "schedule": "0 2 * * *"
    }
  ]
}
