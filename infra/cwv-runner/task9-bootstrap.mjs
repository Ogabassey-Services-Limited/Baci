// biome-ignore-all lint: the sealed first-stage bootstrap is deliberately compact and self-contained
// biome-ignore-all format: the sealed first-stage bundle is intentionally compact and self-contained
import { createHash } from 'node:crypto';
import { closeSync, constants, fchmodSync, fstatSync, fsyncSync, lstatSync, mkdirSync, mkdtempSync, openSync, readdirSync, readSync, renameSync, rmSync, writeSync } from 'node:fs';
import { basename, dirname, join, relative, resolve } from 'node:path';

export const BUNDLE_ENTRIES = Object.freeze(['manifest.json', 'manifest.sha256', 'source.tar', 'source.tar.sha256', 'task9-bootstrap.mjs', 'node', 'node-provenance.json']);
export const TASK9_SOURCE_FILES = Object.freeze(['infra/cwv-runner/archive-index.mjs', 'infra/cwv-runner/archive-link-validation.mjs', 'infra/cwv-runner/archive-stream.mjs', 'infra/cwv-runner/build-image.mjs', 'infra/cwv-runner/campaign-accounting-contract.mjs', 'infra/cwv-runner/campaign-capture-authority.mjs', 'infra/cwv-runner/campaign-cron-tree.mjs', 'infra/cwv-runner/campaign-lease-holder.sh', 'infra/cwv-runner/campaign-network-contract.mjs', 'infra/cwv-runner/campaign-ownership.mjs', 'infra/cwv-runner/campaign-quiesce.sh', 'infra/cwv-runner/campaign-restore-baseline.mjs', 'infra/cwv-runner/campaign-restore-network.mjs', 'infra/cwv-runner/campaign-restore.sh', 'infra/cwv-runner/campaign-source-closure.mjs', 'infra/cwv-runner/campaign-state-collisions.mjs', 'infra/cwv-runner/campaign-state-journal-lock.mjs', 'infra/cwv-runner/campaign-state.mjs', 'infra/cwv-runner/campaign-terminal-cleanup.mjs', 'infra/cwv-runner/campaign-traffic.mjs', 'infra/cwv-runner/canonical-json.mjs', 'infra/cwv-runner/command-settings-contract.mjs', 'infra/cwv-runner/cron-inventory.json', 'infra/cwv-runner/exact-run-accounting.mjs', 'infra/cwv-runner/exact-run-contract-cli.mjs', 'infra/cwv-runner/exact-run-contract.mjs', 'infra/cwv-runner/exact-run-controller.sh', 'infra/cwv-runner/exact-run-live-sample-contract.mjs', 'infra/cwv-runner/exact-run-process-contract.mjs', 'infra/cwv-runner/exact-run-rearm-contract.mjs', 'infra/cwv-runner/exact-run-terminal-cleanup.sh', 'infra/cwv-runner/exact-run-transition-contract.mjs', 'infra/cwv-runner/image-archive-authority.mjs', 'infra/cwv-runner/image-process-map.mjs', 'infra/cwv-runner/image-projection-config.mjs', 'infra/cwv-runner/image-projection.mjs', 'infra/cwv-runner/install-prepare-acceptance.mjs', 'infra/cwv-runner/install-prepare-content-cleanup-cli.mjs', 'infra/cwv-runner/install-prepare-content-cleanup.mjs', 'infra/cwv-runner/install-prepare-content-safety.mjs', 'infra/cwv-runner/install-prepare-runtime-receipt.mjs', 'infra/cwv-runner/install-prepare-store.mjs', 'infra/cwv-runner/owner-api-transport-cli-state.mjs', 'infra/cwv-runner/owner-api-transport-evidence.mjs', 'infra/cwv-runner/owner-api-transport-failure.mjs', 'infra/cwv-runner/owner-api-transport-hold.mjs', 'infra/cwv-runner/owner-api-transport-http.mjs', 'infra/cwv-runner/owner-api-transport-operation-evidence.mjs', 'infra/cwv-runner/owner-api-transport-pagination.mjs', 'infra/cwv-runner/owner-api-transport-primitives.mjs', 'infra/cwv-runner/owner-api-transport-requests.mjs', 'infra/cwv-runner/owner-api-transport-runtime.mjs', 'infra/cwv-runner/owner-api-transport-security.mjs', 'infra/cwv-runner/owner-api-transport-source.mjs', 'infra/cwv-runner/owner-api-transport-zip.mjs', 'infra/cwv-runner/owner-api-transport.mjs', 'infra/cwv-runner/owner-dispatch.sh', 'infra/cwv-runner/policy.schema.mjs', 'infra/cwv-runner/registration-token-mount.mjs', 'infra/cwv-runner/rootfs-projection-contract.mjs', 'infra/cwv-runner/rootfs-source-inventory.mjs', 'infra/cwv-runner/rootfs-source-membership-input.mjs', 'infra/cwv-runner/rootfs-source-membership.mjs', 'infra/cwv-runner/runner-runtime-archive-snapshot.mjs', 'infra/cwv-runner/runner-runtime-identity-manifest.mjs', 'infra/cwv-runner/runner-runtime-manifest-producer.mjs', 'infra/cwv-runner/runner-runtime-manifest-receipt-reader.mjs', 'infra/cwv-runner/runner-runtime-projection.mjs', 'infra/cwv-runner/runner-runtime-receipt-contract.mjs', 'infra/cwv-runner/source-archive.mjs', 'infra/cwv-runner/source-manifest.mjs', 'infra/cwv-runner/source-tree-projection.mjs', 'infra/cwv-runner/task9-bootstrap-runtime.mjs', 'infra/cwv-runner/task9-bootstrap.mjs', 'infra/cwv-runner/task9-owner-documents.mjs', 'infra/cwv-runner/task9-source-authorization.mjs', 'infra/cwv-runner/verify-owner-cli.sh', 'infra/cwv-runner/vps-ssh.sh']);
export const TASK9_OPERATIONS = Object.freeze(['list-attestation-runs', 'dispatch-exact-run', 'read-exact-run', 'cancel-exact-run', 'read-failed-job-evidence', 'rerun-failed-exact-run', 'list-runner-inventory', 'read-exact-job', 'list-exact-artifacts', 'download-exact-artifact']);
const MODES = Object.freeze({'manifest.json':'100400','manifest.sha256':'100400','source.tar':'100400','source.tar.sha256':'100400','task9-bootstrap.mjs':'100400',node:'100500','node-provenance.json':'100400'});
const hash = (bytes) => createHash('sha256').update(bytes).digest('hex');
const fail = (message = 'invalid invocation') => { throw new Error(message); };
const object = (value) => value !== null && typeof value === 'object' && !Array.isArray(value) && Object.getPrototypeOf(value) === Object.prototype;
const sameKeys = (actual, expected) => actual.length === expected.length && actual.every((key,index) => key === expected[index]);
const exact = (value, keys) => object(value) && sameKeys(Object.keys(value).sort(),[...keys].sort());
const digest = (value) => typeof value === 'string' && /^[a-f0-9]{64}$/.test(value);
const sha = (value) => typeof value === 'string' && /^[a-f0-9]{40}$/.test(value);
const same = (left, right) => left.dev === right.dev && left.ino === right.ino;
const native = (mode) => Number.parseInt(mode.slice(3), 8);
const publications = new WeakMap();

export function canonicalJson(value) { if (value === null || ['string','boolean'].includes(typeof value) || typeof value === 'number' && Number.isFinite(value)) return JSON.stringify(value); if (Array.isArray(value)) return `[${value.map(canonicalJson).join(',')}]`; if (!object(value)) fail('unsupported JSON value'); return `{${Object.keys(value).sort().map((key) => `${JSON.stringify(key)}:${canonicalJson(value[key])}`).join(',')}}`; }
export function validSourcePath(path) { return typeof path === 'string' && /^infra\/cwv-runner\/(?:[A-Za-z0-9._@-]+\/)*[A-Za-z0-9._@-]+$/.test(path) && !path.includes('..'); }
function zero(bytes) { return bytes.every((byte) => byte === 0); }
function field(header, start, size) { const bytes = header.subarray(start, start + size), end = bytes.indexOf(0), used = end < 0 ? bytes : bytes.subarray(0,end); if (end >= 0 && !zero(bytes.subarray(end)) || used.some((byte) => byte < 0x20 || byte > 0x7e)) fail('invalid tar'); return used.toString('ascii'); }
function octal(header, start, size) { const value = field(header,start,size); if (!/^[0-7]+$/.test(value)) fail('invalid tar'); const result = Number.parseInt(value,8); if (!Number.isSafeInteger(result)) fail('invalid tar'); return result; }
function checksum(header) { const bytes=header.subarray(148,156); if(bytes[6]!==0||bytes[7]!==32||bytes.subarray(0,6).some((byte)=>byte<48||byte>55)) fail('invalid tar'); const value=Number.parseInt(bytes.subarray(0,6).toString('ascii'),8); if(!Number.isSafeInteger(value)) fail('invalid tar'); return value; }
function encodedOctal(header,start,size,value) { return header.subarray(start,start+size).equals(Buffer.from(`${value.toString(8).padStart(size-1,'0')}\0`)); }
function splitPath(value) { if(Buffer.byteLength(value)<=100) return ['',value]; for(let index=value.lastIndexOf('/');index>0;index=value.lastIndexOf('/',index-1)) { const prefix=value.slice(0,index),name=value.slice(index+1); if(Buffer.byteLength(prefix)<=155&&Buffer.byteLength(name)<=100) return [prefix,name]; } fail('invalid tar'); }
function entries(rows) { if (!Array.isArray(rows) || rows.length > 1024) fail('invalid tar'); let previous = ''; for (const row of rows) { if (!exact(row,['mode','path','sha256']) || !validSourcePath(row.path) || !['100644','100755'].includes(row.mode) || !digest(row.sha256) || row.path <= previous) fail('invalid tar'); previous = row.path; } }
export function parseUstar(archive, expected) { if (!Buffer.isBuffer(archive) || archive.length < 1024 || archive.length > 16*1024*1024 || archive.length % 512) fail('invalid tar'); entries(expected); const rows=[]; let offset=0; while (offset<archive.length) { const header=archive.subarray(offset,offset+512); if (zero(header)) { if (offset+1024!==archive.length || !zero(archive.subarray(offset+512))) fail('invalid tar'); break; } if (rows.length===1024 || header.subarray(257,263).toString('ascii')!=='ustar\0' || header.subarray(263,265).toString('ascii')!=='00' || header[156]!==0 || !zero(header.subarray(157,257)) || !zero(header.subarray(265,329)) || !zero(header.subarray(500,512))) fail('invalid tar'); const stored=checksum(header), actual=header.reduce((sum,byte,index)=>sum+(index>=148&&index<156?32:byte),0); if(stored!==actual) fail('invalid tar'); const name=field(header,0,100), prefix=field(header,345,155), path=prefix?`${prefix}/${name}`:name, parsedMode=octal(header,100,8), mode=`100${parsedMode.toString(8).padStart(3,'0')}`, size=octal(header,124,12), start=offset+512, end=start+size, next=end+(512-size%512)%512, [canonicalPrefix,canonicalName]=splitPath(path); if(!validSourcePath(path)||!['100644','100755'].includes(mode)||name!==canonicalName||prefix!==canonicalPrefix||!encodedOctal(header,100,8,parsedMode)||!encodedOctal(header,108,8,0)||!encodedOctal(header,116,8,0)||!encodedOctal(header,124,12,size)||!encodedOctal(header,136,12,0)||!encodedOctal(header,329,8,0)||!encodedOctal(header,337,8,0)||size>1024*1024||next>archive.length||!zero(archive.subarray(end,next))) fail('invalid tar'); const bytes=Buffer.from(archive.subarray(start,end)); rows.push({bytes,mode,path,sha256:hash(bytes)}); offset=next; } if(offset+1024!==archive.length||rows.length!==expected.length) fail('invalid tar'); for(let index=0;index<rows.length;index+=1) if(rows[index].path!==expected[index].path||rows[index].mode!==expected[index].mode||rows[index].sha256!==expected[index].sha256) fail('invalid tar'); return rows; }
function document(bytes, label) { if(!Buffer.isBuffer(bytes)) fail(`invalid ${label}`); let value; try { value=JSON.parse(bytes.toString('utf8')); } catch { fail(`invalid ${label}`); } if(canonicalJson(value)!==bytes.toString('utf8')) fail(`invalid ${label}`); return value; }
function policyDocument(bytes) { if(!Buffer.isBuffer(bytes)) fail('invalid policy'); try { return JSON.parse(bytes.toString('utf8')); } catch { fail('invalid policy'); } }
function payload(files, owner) { if(!Number.isSafeInteger(owner)||owner<0||!object(files)||!sameKeys(Object.keys(files).sort(),[...BUNDLE_ENTRIES].sort())) fail('invalid payload'); for(const name of BUNDLE_ENTRIES) { const row=files[name]; if(!exact(row,['bytes','mode','owner','symlink'])||!Buffer.isBuffer(row.bytes)||row.mode!==MODES[name]||row.owner!==owner||row.symlink!==false) fail('invalid payload'); } }
function detached(bytes, line, name) { const expected=`${hash(bytes)}  ${name}\n`; if(!Buffer.isBuffer(line)||line.length!==Buffer.byteLength(expected)||line.toString('ascii')!==expected) fail('invalid digest'); }
function binding(envelope) { if(!exact(envelope,['base','bundleId','deploymentSha','exactRun','generation','mergeSha','payload','policy','pullRequest','purpose','ref','repository','reviewedSha','runtime','schemaVersion','source','transactionId','transport'])||envelope.schemaVersion!==1||envelope.purpose!=='task9-exact-run'||!exact(envelope.repository,['id','name'])||!Number.isSafeInteger(envelope.repository.id)||envelope.repository.id<1||typeof envelope.repository.name!=='string'||!/^refs\/(?:heads|pull)\/[A-Za-z0-9._/-]+$/.test(envelope.ref)||![envelope.reviewedSha,envelope.mergeSha,envelope.deploymentSha].every(sha)||!Number.isSafeInteger(envelope.generation)||envelope.generation<0||![envelope.bundleId,envelope.transactionId].every((value)=>typeof value==='string'&&/^[a-z0-9][a-z0-9._-]{0,127}$/.test(value))) fail('invalid envelope'); if(!exact(envelope.base,['ref','sha'])||!/^refs\/heads\/[A-Za-z0-9._/-]+$/.test(envelope.base.ref)||!sha(envelope.base.sha)||!exact(envelope.pullRequest,['headRef','number'])||!Number.isSafeInteger(envelope.pullRequest.number)||envelope.pullRequest.number<1||typeof envelope.pullRequest.headRef!=='string'||!envelope.pullRequest.headRef) fail('invalid envelope'); if(!exact(envelope.exactRun,['admissionId','workflow'])||!digest(envelope.exactRun.admissionId)||!exact(envelope.exactRun.workflow,['id','path','ref'])||!Number.isSafeInteger(envelope.exactRun.workflow.id)||envelope.exactRun.workflow.id<1||envelope.exactRun.workflow.path!=='.github/workflows/cwv-runner-attestation.yml'||envelope.exactRun.workflow.ref!=='refs/heads/main') fail('invalid envelope'); if(!exact(envelope.policy,['path','sha256'])||!exact(envelope.transport,['path','sha256'])||!validSourcePath(envelope.policy.path)||!validSourcePath(envelope.transport.path)||!digest(envelope.policy.sha256)||!digest(envelope.transport.sha256)) fail('invalid envelope'); if(!exact(envelope.runtime,['bootstrapSha256','launcherSha256','nodeProvenanceSha256','nodeSha256','nodeVersion','runtimeSha256'])||![envelope.runtime.bootstrapSha256,envelope.runtime.launcherSha256,envelope.runtime.nodeProvenanceSha256,envelope.runtime.nodeSha256,envelope.runtime.runtimeSha256].every(digest)||!/^v?\d+\.\d+\.\d+$/.test(envelope.runtime.nodeVersion)||!exact(envelope.source,['archiveSha256','manifestSha256'])||![envelope.source.archiveSha256,envelope.source.manifestSha256].every(digest)) fail('invalid envelope'); const {runtimeSha256,...runtime}=envelope.runtime; if(runtimeSha256!==hash(Buffer.from(canonicalJson(runtime)))) fail('invalid envelope'); return envelope; }
function provenance(value,runtime,policy) { if(!exact(value,['archiveSha256','artifact','checksumSha256','executableSha256','keyringSha256','schemaVersion','sha256','signatureSha256','version'])||value.schemaVersion!==1||value.artifact!=='node'||value.sha256!==runtime.nodeSha256||value.executableSha256!==runtime.nodeSha256||value.archiveSha256!==policy?.supplyChain?.node?.ownerDarwinArm64Sha256||value.version!==runtime.nodeVersion||![value.archiveSha256,value.checksumSha256,value.keyringSha256,value.signatureSha256].every(digest)) fail('invalid node provenance'); }
function manifest(value) { if(!exact(value,['authority','baseSha','entries','mergeSha','policyCanonicalSha256','policyFileSha256','prNumber','reviewedHeadSha','schemaVersion','sourceArchive'])||value.schemaVersion!==1||!sha(value.baseSha)||!sha(value.mergeSha)||!sha(value.reviewedHeadSha)||!digest(value.policyCanonicalSha256)||!digest(value.policyFileSha256)||!Number.isSafeInteger(value.prNumber)||value.prNumber<1||!object(value.authority)||!Array.isArray(value.entries)||!exact(value.sourceArchive,['entries','prefix'])||value.sourceArchive.prefix!=='infra/cwv-runner/'||!Array.isArray(value.sourceArchive.entries)) fail('invalid manifest'); for(const row of value.entries) { if(!object(row)||typeof row.path!=='string'||!['A','M','D'].includes(row.status)||row.status==='D'&&!exact(row,['absent','path','status'])||row.status!=='D'&&!exact(row,['blobSha256','mode','path','status'])||row.status==='D'&&row.absent!==true||row.status!=='D'&&!['100644','100755'].includes(row.mode)||row.status!=='D'&&!digest(row.blobSha256)) fail('invalid manifest'); } for(const row of value.sourceArchive.entries) if(!exact(row,['blobSha256','mode','path'])) fail('invalid manifest'); return value; }
function reviewedPayload(envelope,files) { if(!exact(envelope.payload,['entries'])||!Array.isArray(envelope.payload.entries)||envelope.payload.entries.length!==BUNDLE_ENTRIES.length) fail('invalid reviewed payload'); const names=[...BUNDLE_ENTRIES].sort(); for(let index=0;index<names.length;index+=1) { const name=names[index], row=envelope.payload.entries[index]; if(!exact(row,['mode','path','sha256','type'])||row.path!==`payload/${name}`||row.type!=='file'||row.mode!==MODES[name]||row.sha256!==hash(files[name].bytes)) fail('invalid reviewed payload'); } if(envelope.source.archiveSha256!==hash(files['source.tar'].bytes)||envelope.source.manifestSha256!==hash(files['manifest.json'].bytes)) fail('invalid reviewed payload'); }
export function assertTask9SourceFiles(rows) { if(!Array.isArray(rows)||rows.length!==TASK9_SOURCE_FILES.length||rows.some((row)=>!exact(row,['path','sha256'])||!digest(row.sha256))||rows.map(({path})=>path).join('\n')!==TASK9_SOURCE_FILES.join('\n')) fail('invalid source archive'); }
function sourceRows(archive,value,envelope) { const sourceManifest=manifest(value), expected=sourceManifest.sourceArchive.entries.map(({blobSha256:sha256,...row})=>({...row,sha256})), rows=parseUstar(archive,expected), byPath=new Map(rows.map((row)=>[row.path,row])); for(const [path,expectedHash] of [[envelope.policy.path,envelope.policy.sha256],[envelope.transport.path,envelope.transport.sha256],['infra/cwv-runner/task9-bootstrap-runtime.mjs',envelope.runtime.launcherSha256],['infra/cwv-runner/task9-bootstrap.mjs',envelope.runtime.bootstrapSha256]]) if(byPath.get(path)?.sha256!==expectedHash) fail('invalid source archive'); const policy=policyDocument(byPath.get(envelope.policy.path)?.bytes); if(!object(policy.authority)||[sourceManifest.prNumber,sourceManifest.baseSha,sourceManifest.reviewedHeadSha,sourceManifest.mergeSha,sourceManifest.policyFileSha256,sourceManifest.policyCanonicalSha256,canonicalJson(sourceManifest.authority)].some((actual,index)=>actual!==[envelope.pullRequest.number,envelope.base.sha,envelope.reviewedSha,envelope.mergeSha,envelope.policy.sha256,hash(Buffer.from(canonicalJson(policy))),canonicalJson(policy.authority)][index])) fail('invalid source binding'); assertTask9SourceFiles(TASK9_SOURCE_FILES.map((path)=>({path,sha256:byPath.get(path)?.sha256}))); return rows; }
export function authorizeTask9Bundle(input) { const files=input?.files, envelopeBytes=input?.envelopeBytes; payload(files,input?.owner); if(!Buffer.isBuffer(envelopeBytes)||!digest(input?.envelopeSha256)||!digest(input?.reviewedEnvelopeSha256)||input.envelopeSha256!==input.reviewedEnvelopeSha256||hash(envelopeBytes)!==input.envelopeSha256) fail('invalid envelope digest'); const envelope=binding(document(envelopeBytes,'envelope')); if(envelope.bundleId!==input.bundleId) fail('invalid bundle id'); reviewedPayload(envelope,files); detached(files['manifest.json'].bytes,files['manifest.sha256'].bytes,'manifest.json'); detached(files['source.tar'].bytes,files['source.tar.sha256'].bytes,'source.tar'); if(hash(files.node.bytes)!==envelope.runtime.nodeSha256||hash(files['task9-bootstrap.mjs'].bytes)!==envelope.runtime.bootstrapSha256||hash(files['node-provenance.json'].bytes)!==envelope.runtime.nodeProvenanceSha256) fail('invalid node provenance'); const rows=sourceRows(files['source.tar'].bytes,manifest(document(files['manifest.json'].bytes,'manifest')),envelope), byPath=new Map(rows.map((row)=>[row.path,row.sha256])), policy=policyDocument(rows.find(({path})=>path===envelope.policy.path).bytes); provenance(document(files['node-provenance.json'].bytes,'node provenance'),envelope.runtime,policy); const receipt={generation:envelope.generation,operationSet:TASK9_OPERATIONS,operationSetDigest:hash(Buffer.from(canonicalJson(TASK9_OPERATIONS))),policyFileSha256:envelope.policy.sha256,provenance:{manifestSha256:hash(files['manifest.json'].bytes),nodeProvenanceSha256:envelope.runtime.nodeProvenanceSha256,runtimeSha256:envelope.runtime.runtimeSha256,sourceArchiveSha256:envelope.source.archiveSha256},purpose:envelope.purpose,schemaVersion:1,sourceBinding:{base:envelope.base,deploymentSha:envelope.deploymentSha,exactRun:envelope.exactRun,mergeSha:envelope.mergeSha,pullRequest:envelope.pullRequest,ref:envelope.ref,repository:envelope.repository,reviewedSha:envelope.reviewedSha},sourceFiles:TASK9_SOURCE_FILES.map((path)=>({path,sha256:byPath.get(path)})),transactionId:envelope.transactionId}; const receiptBytes=Buffer.from(canonicalJson(receipt)), tree=rows.map((row)=>({bytes:Buffer.from(row.bytes),mode:row.mode,path:row.path})), authorized=Object.freeze({receipt:Object.freeze(receipt),receiptBytes:Buffer.from(receiptBytes),tree:new Map(tree.map((row)=>[row.path,{bytes:row.bytes,mode:row.mode}]))}); publications.set(authorized,{receiptBytes,tree}); return authorized; }
export function publishAuthorizedTree(authorized,write) { const publication=publications.get(authorized); if(!publication||typeof write!=='function') fail('invalid publication'); for(const entry of publication.tree) write(entry.path,Buffer.from(entry.bytes),entry.mode); write('receipt.json',Buffer.from(publication.receiptBytes),'100400'); }
function held(path,owner,mode) { const before=lstatSync(path); if(before.isSymbolicLink()||!before.isFile()||mode!==undefined&&before.mode.toString(8)!==mode) fail(); let fd; try { fd=openSync(path,constants.O_RDONLY|constants.O_NOFOLLOW); const stat=fstatSync(fd); if(!stat.isFile()||!same(before,stat)||stat.uid!==owner) fail(); const bytes=Buffer.alloc(stat.size); for(let offset=0;offset<bytes.length;) { const count=readSync(fd,bytes,offset,bytes.length-offset,offset); if(count<1) fail(); offset+=count; } if(!same(stat,lstatSync(path))) fail(); return {bytes,fd,mode:stat.mode.toString(8),owner:stat.uid,symlink:false}; } catch(error) { if(fd!==undefined) closeSync(fd); throw error; } }
function closed(path,owner,mode) { const file=held(path,owner,mode); try { return {bytes:file.bytes,mode:file.mode,owner:file.owner,symlink:false}; } finally { closeSync(file.fd); } }
function directory(path) { const stat=lstatSync(path); if(stat.isSymbolicLink()||!stat.isDirectory()) fail(); }
export function readBundleFiles(bundleDir,owner) { directory(bundleDir); if(!sameKeys(readdirSync(bundleDir).sort(),[...BUNDLE_ENTRIES].sort())) fail(); return Object.fromEntries(BUNDLE_ENTRIES.map((name)=>[name,closed(join(bundleDir,name),owner,MODES[name])])); }
export function assertPinnedExecution(runtime,files) { if(!runtime||!digest(runtime.nodeSha256)||!digest(runtime.bootstrapSha256)||!files||!Buffer.isBuffer(files.nodeBytes)||!Buffer.isBuffer(files.bootstrapBytes)||hash(files.nodeBytes)!==runtime.nodeSha256||hash(files.bootstrapBytes)!==runtime.bootstrapSha256) fail(); }
export function withPinnedTask9Execution(runtime,{bootstrapPath,nodePath,owner},consume=()=>undefined) { const bootstrap=held(resolve(bootstrapPath),owner,'100400'); let node; try { node=held(resolve(nodePath),owner,'100500'); assertPinnedExecution(runtime,{bootstrapBytes:bootstrap.bytes,nodeBytes:node.bytes}); return consume({bootstrapBytes:Buffer.from(bootstrap.bytes),descriptors:{bootstrap:bootstrap.fd,node:node.fd},nodeBytes:Buffer.from(node.bytes)}); } finally { if(node) closeSync(node.fd); closeSync(bootstrap.fd); } }
function sync(path) { const fd=openSync(path,constants.O_RDONLY|constants.O_DIRECTORY|constants.O_NOFOLLOW); try { fsyncSync(fd); } finally { closeSync(fd); } }
function writeTree(root,path,bytes,mode,owner) { const target=resolve(root,path); if(relative(root,target).startsWith('..')) fail(); mkdirSync(dirname(target),{recursive:true,mode:0o700}); const fd=openSync(target,constants.O_CREAT|constants.O_EXCL|constants.O_WRONLY|constants.O_NOFOLLOW,0o600); try { for(let offset=0;offset<bytes.length;) { const count=writeSync(fd,bytes,offset,bytes.length-offset); if(count<1) fail(); offset+=count; } fchmodSync(fd,native(mode)); fsyncSync(fd); } finally { closeSync(fd); } const stat=lstatSync(target); if(stat.isSymbolicLink()||!stat.isFile()||stat.uid!==owner||stat.mode.toString(8)!==mode) fail(); }
function syncTree(path) { for(const entry of readdirSync(path,{withFileTypes:true})) { if(entry.isSymbolicLink()) fail(); if(entry.isDirectory()) syncTree(join(path,entry.name)); } sync(path); }
function publish(authorized,target,owner) { const parent=dirname(target); directory(parent); if(lstatSync(target,{throwIfNoEntry:false})) fail(); const temporary=mkdtempSync(join(parent,`.${basename(target)}.tmp-`)); try { const publication=publications.get(authorized); for(const row of publication.tree) writeTree(temporary,row.path,row.bytes,row.mode,owner); const receipt=publication.receiptBytes, treeDigest=hash(Buffer.from(canonicalJson(publication.tree.map(({mode,path,bytes})=>({mode,path,sha256:hash(bytes)}))))); writeTree(temporary,'source-authorization.json',receipt,'100400',owner); writeTree(temporary,'source-authorization.sha256',Buffer.from(`${hash(receipt)}\n`),'100400',owner); writeTree(temporary,'source-tree.sha256',Buffer.from(`${treeDigest}\n`),'100400',owner); writeTree(temporary,'receipt.json',receipt,'100400',owner); syncTree(temporary); sync(parent); renameSync(temporary,target); sync(parent); if(hash(closed(join(target,'source-authorization.json'),owner,'100400').bytes)!==hash(receipt)||closed(join(target,'source-tree.sha256'),owner,'100400').bytes.toString('ascii')!==`${treeDigest}\n`) fail(); } catch(error) { rmSync(temporary,{force:true,recursive:true}); throw error; } }
export function parseBootstrapArgs(argv) { if(argv.length!==15||argv[0]!=='--authorize'||argv[1]!=='--bundle-id'||argv[3]!=='--reviewed-envelope-sha256'||argv[5]!=='--reviewed-launcher-sha256'||argv[7]!=='--bundle-dir'||argv[9]!=='--envelope'||argv[11]!=='--envelope-sha256'||argv[13]!=='--owner') fail(); const owner=Number(argv[14]); if(!Number.isSafeInteger(owner)||owner<0||!digest(argv[4])||!digest(argv[6])||![argv[2],argv[8],argv[10],argv[12]].every((value)=>typeof value==='string'&&value)) fail(); return {bundleDir:resolve(argv[8]),bundleId:argv[2],envelope:resolve(argv[10]),envelopeSha256Path:resolve(argv[12]),owner,publishDir:join(dirname(resolve(argv[8])),'authorized-source'),reviewedEnvelopeSha256:argv[4],reviewedLauncherSha256:argv[6]}; }
export function runBootstrapCli(argv,verified) { const args=parseBootstrapArgs(argv); if(process.getuid()!==args.owner||!exact(verified,['bundleDir','digestBytes','envelopeBytes','files','owner'])||verified.bundleDir!==args.bundleDir||verified.owner!==args.owner||!Buffer.isBuffer(verified.envelopeBytes)||!Buffer.isBuffer(verified.digestBytes)) fail(); const files=verified.files,envelopeBytes=verified.envelopeBytes,expected=`${args.reviewedEnvelopeSha256}\n`; if(hash(envelopeBytes)!==args.reviewedEnvelopeSha256||verified.digestBytes.toString('ascii')!==expected) fail(); const parsed=document(envelopeBytes,'envelope'); if(parsed.runtime.launcherSha256!==args.reviewedLauncherSha256) fail(); const authorized=authorizeTask9Bundle({bundleId:args.bundleId,envelopeBytes,envelopeSha256:hash(envelopeBytes),files,owner:args.owner,reviewedEnvelopeSha256:args.reviewedEnvelopeSha256}); publish(authorized,args.publishDir,args.owner); return authorized.receipt; }
if(process.argv[1]?.endsWith('/task9-bootstrap.mjs')) { process.stderr.write('task9 bootstrap refused: trusted launcher required\n'); process.exitCode=1; }
