--- apps/mobile-storefront/hooks/use-network-state.ts
+++ apps/mobile-storefront/hooks/use-network-state.ts
@@ -8,6 +8,7 @@
  */

 import NetInfo, { type NetInfoState } from '@react-native-community/netinfo';
+import { onlineManager } from '@tanstack/react-query';
 import { useEffect, useRef, useState } from 'react';
 import { createLogger } from '@/lib/logger';

@@ -70,6 +71,8 @@
       const isInternetReachable = netState.isInternetReachable ?? true;
       const isOnline = isConnected && isInternetReachable !== false;

+      onlineManager.setOnline(isOnline);
+
       // Detect reconnection
       const wasJustReconnected = wasOffline.current && isOnline;

@@ -118,6 +121,8 @@
       const isInternetReachable = netState.isInternetReachable ?? true;
       const isOnline = isConnected && isInternetReachable !== false;

+      onlineManager.setOnline(isOnline);
+
       setState({
         isConnected,
         isInternetReachable,
@@ -144,6 +149,8 @@
     const isConnected = netState.isConnected ?? true;
     const isInternetReachable = netState.isInternetReachable ?? true;
     const isOnline = isConnected && isInternetReachable !== false;
+
+    onlineManager.setOnline(isOnline);

     setState((prev) => ({
       ...prev,
