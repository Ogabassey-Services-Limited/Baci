diff --git a/android/build.gradle b/android/build.gradle
index 6348d17da..0560231f1 100644
--- a/android/build.gradle
+++ b/android/build.gradle
@@ -11,7 +11,7 @@ buildscript {

     dependencies {
         classpath("org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlin_version")
-        classpath("com.android.tools.build:gradle:8.10.1")
+        classpath("com.android.tools.build:gradle:9.1.0")
         classpath("com.diffplug.spotless:spotless-plugin-gradle:7.0.4")
     }
 }
@@ -62,7 +62,10 @@ if (isNewArchitectureEnabled()) {
 }

 apply plugin: 'com.android.library'
-apply plugin: 'kotlin-android'
+def builtInKotlinEnabled = !project.hasProperty("android.builtInKotlin") || project.property("android.builtInKotlin").toString().toBoolean()
+if (!builtInKotlinEnabled) {
+    apply plugin: 'org.jetbrains.kotlin.android'
+}

 if (project == rootProject) {
     apply from: "spotless.gradle"
@@ -127,7 +130,7 @@ repositories {
 }

 android {
-    compileSdkVersion safeExtGet("compileSdkVersion", 33)
+    compileSdk safeExtGet("compileSdkVersion", 33)

     namespace = "com.swmansion.gesturehandler"
     buildFeatures {
@@ -145,8 +148,8 @@ android {
     }

     defaultConfig {
-        minSdkVersion safeExtGet('minSdkVersion', 24)
-        targetSdkVersion safeExtGet('targetSdkVersion', 33)
+        minSdk safeExtGet('minSdkVersion', 24)
+        targetSdk safeExtGet('targetSdkVersion', 33)
         buildConfigField "boolean", "IS_NEW_ARCHITECTURE_ENABLED", isNewArchitectureEnabled().toString()
         buildConfigField "int", "REACT_NATIVE_MINOR_VERSION", REACT_NATIVE_MINOR_VERSION.toString()

@@ -193,22 +196,44 @@ android {
         java {
             if (shouldUseCommonInterfaceFromReanimated()) {
                 srcDirs += 'reanimated/src/main/java'
             } else {
                 srcDirs += 'noreanimated/src/main/java'
             }

             if (shouldUseCommonInterfaceFromRNSVG()) {
                 srcDirs += 'svg/src/main/java'
             } else {
                 srcDirs += 'nosvg/src/main/java'
             }

             if (isNewArchitectureEnabled()) {
                 srcDirs += 'fabric/src/main/java'
             } else {
                 // 'paper/src/main/java' includes files from codegen so the library can compile with
                 // codegen turned off
                 srcDirs += 'paper/src/main/java'
             }
         }
     }
+
+    sourceSets.named("main") {
+        kotlin.directories.add("src/main/java")
+
+        if (shouldUseCommonInterfaceFromReanimated()) {
+            kotlin.directories.add("reanimated/src/main/java")
+        } else {
+            kotlin.directories.add("noreanimated/src/main/java")
+        }
+
+        if (shouldUseCommonInterfaceFromRNSVG()) {
+            kotlin.directories.add("svg/src/main/java")
+        } else {
+            kotlin.directories.add("nosvg/src/main/java")
+        }
+
+        if (isNewArchitectureEnabled()) {
+            kotlin.directories.add("fabric/src/main/java")
+        } else {
+            kotlin.directories.add("paper/src/main/java")
+        }
+    }
diff --git a/android/src/main/java/com/swmansion/gesturehandler/react/RNGestureHandlerModule.kt b/android/src/main/java/com/swmansion/gesturehandler/react/RNGestureHandlerModule.kt
index e149fa49f..8cab96133 100644
--- a/android/src/main/java/com/swmansion/gesturehandler/react/RNGestureHandlerModule.kt
+++ b/android/src/main/java/com/swmansion/gesturehandler/react/RNGestureHandlerModule.kt
@@ -170,7 +170,7 @@ class RNGestureHandlerModule(reactContext: ReactApplicationContext?) :
     }
     synchronized(roots) {
       return roots.firstOrNull {
-        it.rootView is ReactRootView && it.rootView.rootViewTag == rootViewTag
+        it.rootView is ReactRootView && it.rootView.getRootViewTag() == rootViewTag
       }
     }
   }
