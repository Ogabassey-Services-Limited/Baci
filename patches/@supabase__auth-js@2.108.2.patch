diff --git a/dist/main/GoTrueClient.d.ts b/dist/main/GoTrueClient.d.ts
index 5e2b19e6237548c849c2e9b2fb7386c9da66004c..e6f7a4d2bae8ad040660ab08bc9625994bd5a186 100644
--- a/dist/main/GoTrueClient.d.ts
+++ b/dist/main/GoTrueClient.d.ts
@@ -1936,9 +1936,11 @@ export default class GoTrueClient {
      */
     refreshSession(currentSession?: {
         refresh_token: string;
+        require_storage_match?: boolean;
     }): Promise<AuthResponse>;
     protected _refreshSession(currentSession?: {
         refresh_token: string;
+        require_storage_match?: boolean;
     }): Promise<AuthResponse>;
     /**
      * Gets the session data from a URL string
diff --git a/dist/main/GoTrueClient.js b/dist/main/GoTrueClient.js
index 9af56f25432ca2cf0761049df156cf763ae77a48..6a60fdacd5cb979418baa208d7632c1e34e3836f 100644
--- a/dist/main/GoTrueClient.js
+++ b/dist/main/GoTrueClient.js
@@ -3132,15 +3132,23 @@ class GoTrueClient {
     }
     async _refreshSession(currentSession) {
         try {
+            if (currentSession === null || currentSession === void 0 ? void 0 : currentSession.refresh_token) {
+                const { data: session, error } = await this._callRefreshToken(currentSession.refresh_token, currentSession.require_storage_match === true);
+                if (error) {
+                    return this._returnResult({ data: { user: null, session: null }, error });
+                }
+                return this._returnResult({
+                    data: { user: (session === null || session === void 0 ? void 0 : session.user) || null, session: session || null },
+                    error: null,
+                });
+            }
             return await this._useSession(async (result) => {
                 var _a;
-                if (!currentSession) {
-                    const { data, error } = result;
-                    if (error) {
-                        throw error;
-                    }
-                    currentSession = (_a = data.session) !== null && _a !== void 0 ? _a : undefined;
+                const { data, error: sessionError } = result;
+                if (sessionError) {
+                    throw sessionError;
                 }
+                currentSession = (_a = data.session) !== null && _a !== void 0 ? _a : undefined;
                 if (!(currentSession === null || currentSession === void 0 ? void 0 : currentSession.refresh_token)) {
                     throw new errors_1.AuthSessionMissingError();
                 }
@@ -3893,7 +3901,7 @@ class GoTrueClient {
      * Generates a new JWT.
      * @param refreshToken A valid refresh token that was returned on login.
      */
-    async _refreshAccessToken(refreshToken) {
+    async _refreshAccessToken(refreshToken, allowRetry = true) {
         // Refresh tokens are long-lived bearer credentials; do NOT include any
         // fragment of the token in the debug tag, even when `debug: true` is
         // enabled (logs may be forwarded to third-party services).
@@ -3916,6 +3924,8 @@ class GoTrueClient {
                 const nextBackOffInterval = 200 * Math.pow(2, attempt);
                 return (error &&
                     (0, errors_1.isAuthRetryableFetchError)(error) &&
+                    allowRetry &&
+                    error.status !== 408 &&
                     // retryable only if the request can be sent before the backoff overflows the tick duration
                     Date.now() + nextBackOffInterval - startedAt < constants_1.AUTO_REFRESH_TICK_DURATION_MS);
             });
@@ -4054,7 +4064,7 @@ class GoTrueClient {
             this._debug(debugName, 'end');
         }
     }
-    async _callRefreshToken(refreshToken) {
+    async _callRefreshToken(refreshToken, requireStorageMatch = false) {
         var _a, _b;
         if (!refreshToken) {
             throw new errors_1.AuthSessionMissingError();
@@ -4098,7 +4108,15 @@ class GoTrueClient {
             // cases the guard fires only when storage was *modified between
             // snapshots*, not when the input token disagrees with what's stored.
             const storedAtStart = (await (0, helpers_1.getItemAsync)(this.storage, this.storageKey));
-            const { data, error } = await this._refreshAccessToken(refreshToken);
+            if (requireStorageMatch && (storedAtStart === null || storedAtStart === void 0 ? void 0 : storedAtStart.refresh_token) !== refreshToken) {
+                const discarded = {
+                    data: null,
+                    error: new errors_1.AuthRefreshDiscardedError(),
+                };
+                this.refreshingDeferred.resolve(discarded);
+                return discarded;
+            }
+            const { data, error } = await this._refreshAccessToken(refreshToken, !requireStorageMatch);
             if (error)
                 throw error;
             if (!data.session)
diff --git a/dist/main/lib/fetch.js b/dist/main/lib/fetch.js
index 96c52a133ff2a0608e8afeb8b063625484ed926a..91d77386191d28fe0758178b9e361d9a40631bb6 100644
--- a/dist/main/lib/fetch.js
+++ b/dist/main/lib/fetch.js
@@ -37,7 +37,7 @@ async function handleError(error) {
     if (!(0, helpers_1.looksLikeFetchResponse)(error)) {
         throw new errors_1.AuthRetryableFetchError(_getErrorMessage(error), 0);
     }
-    if (NETWORK_ERROR_CODES.includes(error.status)) {
+    if (error.status === 408 || NETWORK_ERROR_CODES.includes(error.status)) {
         // status in 500...599 range - server had an error, request might be retryed.
         throw new errors_1.AuthRetryableFetchError(_getErrorMessage(error), error.status);
     }
diff --git a/dist/module/GoTrueClient.d.ts b/dist/module/GoTrueClient.d.ts
index 5e2b19e6237548c849c2e9b2fb7386c9da66004c..e6f7a4d2bae8ad040660ab08bc9625994bd5a186 100644
--- a/dist/module/GoTrueClient.d.ts
+++ b/dist/module/GoTrueClient.d.ts
@@ -1936,9 +1936,11 @@ export default class GoTrueClient {
      */
     refreshSession(currentSession?: {
         refresh_token: string;
+        require_storage_match?: boolean;
     }): Promise<AuthResponse>;
     protected _refreshSession(currentSession?: {
         refresh_token: string;
+        require_storage_match?: boolean;
     }): Promise<AuthResponse>;
     /**
      * Gets the session data from a URL string
diff --git a/dist/module/GoTrueClient.js b/dist/module/GoTrueClient.js
index 2ec11102fe38c88918cc7abc0d0e2f100938759a..a659bbdc11f0c8abb8526dd2ad42859bcc77e1ce 100644
--- a/dist/module/GoTrueClient.js
+++ b/dist/module/GoTrueClient.js
@@ -3129,15 +3129,23 @@ class GoTrueClient {
     }
     async _refreshSession(currentSession) {
         try {
+            if (currentSession === null || currentSession === void 0 ? void 0 : currentSession.refresh_token) {
+                const { data: session, error } = await this._callRefreshToken(currentSession.refresh_token, currentSession.require_storage_match === true);
+                if (error) {
+                    return this._returnResult({ data: { user: null, session: null }, error });
+                }
+                return this._returnResult({
+                    data: { user: (session === null || session === void 0 ? void 0 : session.user) || null, session: session || null },
+                    error: null,
+                });
+            }
             return await this._useSession(async (result) => {
                 var _a;
-                if (!currentSession) {
-                    const { data, error } = result;
-                    if (error) {
-                        throw error;
-                    }
-                    currentSession = (_a = data.session) !== null && _a !== void 0 ? _a : undefined;
+                const { data, error: sessionError } = result;
+                if (sessionError) {
+                    throw sessionError;
                 }
+                currentSession = (_a = data.session) !== null && _a !== void 0 ? _a : undefined;
                 if (!(currentSession === null || currentSession === void 0 ? void 0 : currentSession.refresh_token)) {
                     throw new AuthSessionMissingError();
                 }
@@ -3890,7 +3898,7 @@ class GoTrueClient {
      * Generates a new JWT.
      * @param refreshToken A valid refresh token that was returned on login.
      */
-    async _refreshAccessToken(refreshToken) {
+    async _refreshAccessToken(refreshToken, allowRetry = true) {
         // Refresh tokens are long-lived bearer credentials; do NOT include any
         // fragment of the token in the debug tag, even when `debug: true` is
         // enabled (logs may be forwarded to third-party services).
@@ -3913,6 +3921,8 @@ class GoTrueClient {
                 const nextBackOffInterval = 200 * Math.pow(2, attempt);
                 return (error &&
                     isAuthRetryableFetchError(error) &&
+                    allowRetry &&
+                    error.status !== 408 &&
                     // retryable only if the request can be sent before the backoff overflows the tick duration
                     Date.now() + nextBackOffInterval - startedAt < AUTO_REFRESH_TICK_DURATION_MS);
             });
@@ -4051,7 +4061,7 @@ class GoTrueClient {
             this._debug(debugName, 'end');
         }
     }
-    async _callRefreshToken(refreshToken) {
+    async _callRefreshToken(refreshToken, requireStorageMatch = false) {
         var _a, _b;
         if (!refreshToken) {
             throw new AuthSessionMissingError();
@@ -4095,7 +4105,15 @@ class GoTrueClient {
             // cases the guard fires only when storage was *modified between
             // snapshots*, not when the input token disagrees with what's stored.
             const storedAtStart = (await getItemAsync(this.storage, this.storageKey));
-            const { data, error } = await this._refreshAccessToken(refreshToken);
+            if (requireStorageMatch && (storedAtStart === null || storedAtStart === void 0 ? void 0 : storedAtStart.refresh_token) !== refreshToken) {
+                const discarded = {
+                    data: null,
+                    error: new AuthRefreshDiscardedError(),
+                };
+                this.refreshingDeferred.resolve(discarded);
+                return discarded;
+            }
+            const { data, error } = await this._refreshAccessToken(refreshToken, !requireStorageMatch);
             if (error)
                 throw error;
             if (!data.session)
diff --git a/dist/module/lib/fetch.js b/dist/module/lib/fetch.js
index 1e0b329acbdd79509a646b3251c327e5156249b1..cbee901ac4606a68709c7c75593f842042e2f741 100644
--- a/dist/module/lib/fetch.js
+++ b/dist/module/lib/fetch.js
@@ -27,7 +27,7 @@ export async function handleError(error) {
     if (!looksLikeFetchResponse(error)) {
         throw new AuthRetryableFetchError(_getErrorMessage(error), 0);
     }
-    if (NETWORK_ERROR_CODES.includes(error.status)) {
+    if (error.status === 408 || NETWORK_ERROR_CODES.includes(error.status)) {
         // status in 500...599 range - server had an error, request might be retryed.
         throw new AuthRetryableFetchError(_getErrorMessage(error), error.status);
     }
diff --git a/src/GoTrueClient.ts b/src/GoTrueClient.ts
index 9891653c2a4344bb4862d08a1dc33bbc83e873a4..3bbf75cc1039b2ff5b9030c6d97079f50a69cb87 100644
--- a/src/GoTrueClient.ts
+++ b/src/GoTrueClient.ts
@@ -3689,7 +3689,10 @@ export default class GoTrueClient {
    * const { session, user } = data
    * ```
    */
-  async refreshSession(currentSession?: { refresh_token: string }): Promise<AuthResponse> {
+  async refreshSession(currentSession?: {
+    refresh_token: string
+    require_storage_match?: boolean
+  }): Promise<AuthResponse> {
     await this.initializePromise

     if (this.lock != null) {
@@ -3704,18 +3707,32 @@ export default class GoTrueClient {

   protected async _refreshSession(currentSession?: {
     refresh_token: string
+    require_storage_match?: boolean
   }): Promise<AuthResponse> {
     try {
-      return await this._useSession(async (result) => {
-        if (!currentSession) {
-          const { data, error } = result
-          if (error) {
-            throw error
-          }
+      if (currentSession?.refresh_token) {
+        const { data: session, error } = await this._callRefreshToken(
+          currentSession.refresh_token,
+          currentSession.require_storage_match === true
+        )
+        if (error) {
+          return this._returnResult({ data: { user: null, session: null }, error })
+        }
+
+        return this._returnResult({
+          data: { user: session?.user ?? null, session: session ?? null },
+          error: null,
+        })
+      }

-          currentSession = data.session ?? undefined
+      return await this._useSession(async (result) => {
+        const { data, error: sessionError } = result
+        if (sessionError) {
+          throw sessionError
         }

+        currentSession = data.session ?? undefined
+
         if (!currentSession?.refresh_token) {
           throw new AuthSessionMissingError()
         }
@@ -4621,7 +4638,10 @@ export default class GoTrueClient {
    * Generates a new JWT.
    * @param refreshToken A valid refresh token that was returned on login.
    */
-  private async _refreshAccessToken(refreshToken: string): Promise<AuthResponse> {
+  private async _refreshAccessToken(
+    refreshToken: string,
+    allowRetry = true
+  ): Promise<AuthResponse> {
     // Refresh tokens are long-lived bearer credentials; do NOT include any
     // fragment of the token in the debug tag, even when `debug: true` is
     // enabled (logs may be forwarded to third-party services).
@@ -4651,6 +4671,8 @@ export default class GoTrueClient {
           return (
             error &&
             isAuthRetryableFetchError(error) &&
+            allowRetry &&
+            error.status !== 408 &&
             // retryable only if the request can be sent before the backoff overflows the tick duration
             Date.now() + nextBackOffInterval - startedAt < AUTO_REFRESH_TICK_DURATION_MS
           )
@@ -4830,7 +4852,10 @@ export default class GoTrueClient {
     }
   }

-  private async _callRefreshToken(refreshToken: string): Promise<CallRefreshTokenResult> {
+  private async _callRefreshToken(
+    refreshToken: string,
+    requireStorageMatch = false
+  ): Promise<CallRefreshTokenResult> {
     if (!refreshToken) {
       throw new AuthSessionMissingError()
     }
@@ -4882,7 +4907,19 @@ export default class GoTrueClient {
       // snapshots*, not when the input token disagrees with what's stored.
       const storedAtStart = (await getItemAsync(this.storage, this.storageKey)) as Session | null

-      const { data, error } = await this._refreshAccessToken(refreshToken)
+      if (requireStorageMatch && storedAtStart?.refresh_token !== refreshToken) {
+        const discarded: CallRefreshTokenResult = {
+          data: null,
+          error: new AuthRefreshDiscardedError(),
+        }
+        this.refreshingDeferred.resolve(discarded)
+        return discarded
+      }
+
+      const { data, error } = await this._refreshAccessToken(
+        refreshToken,
+        !requireStorageMatch
+      )
       if (error) throw error
       if (!data.session) throw new AuthSessionMissingError()

diff --git a/src/lib/fetch.ts b/src/lib/fetch.ts
index cdef4a8a5c0567eeafc69c2592f05952dd99c60e..deed48a3f13fb1508a72baa31a45afd15ede4b50 100644
--- a/src/lib/fetch.ts
+++ b/src/lib/fetch.ts
@@ -81,7 +81,7 @@ export async function handleError(error: unknown) {
     throw new AuthRetryableFetchError(_getErrorMessage(error), 0)
   }

-  if (NETWORK_ERROR_CODES.includes(error.status)) {
+  if (error.status === 408 || NETWORK_ERROR_CODES.includes(error.status)) {
     // status in 500...599 range - server had an error, request might be retryed.
     throw new AuthRetryableFetchError(_getErrorMessage(error), error.status)
   }
