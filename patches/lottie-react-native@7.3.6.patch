diff --git a/android/build.gradle b/android/build.gradle
index a0dc14ff6..d70ebcb6c 100644
--- a/android/build.gradle
+++ b/android/build.gradle
@@ -8,7 +8,7 @@ buildscript {
     }

     dependencies {
-        classpath("com.android.tools.build:gradle:7.2.2")
+        classpath("com.android.tools.build:gradle:9.1.0")
         classpath("org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlin_version")
     }
 }
@@ -26,7 +26,10 @@ def isNewArchitectureEnabled() {
 }

 apply plugin: 'com.android.library'
-apply plugin: 'kotlin-android'
+def builtInKotlinEnabled = !project.hasProperty("android.builtInKotlin") || project.property("android.builtInKotlin").toString().toBoolean()
+if (!builtInKotlinEnabled) {
+    apply plugin: 'org.jetbrains.kotlin.android'
+}

 if (isNewArchitectureEnabled()) {
     apply plugin: 'com.facebook.react'
@@ -53,8 +56,8 @@ android {
     }

     defaultConfig {
-        minSdkVersion getExtOrDefault('minSdkVersion', 21)
-        targetSdkVersion getExtOrDefault('targetSdkVersion', 31)
+        minSdk getExtOrDefault('minSdkVersion', 21)
+        targetSdk getExtOrDefault('targetSdkVersion', 31)
         buildConfigField "boolean", "IS_NEW_ARCHITECTURE_ENABLED", isNewArchitectureEnabled().toString()

         ndk {
@@ -95,8 +98,10 @@ android {
         main {
             if (isNewArchitectureEnabled()) {
                 java.srcDirs += ['src/newarch', "${project.buildDir}/generated/source/codegen/java"]
+                kotlin.srcDirs += ['src/newarch', "${project.buildDir}/generated/source/codegen/java"]
             } else {
                 java.srcDirs += ['src/oldarch']
+                kotlin.srcDirs += ['src/oldarch']
             }
         }
     }
@@ -135,4 +140,4 @@ if (isNewArchitectureEnabled()) {
     libraryName = 'lottiereactnative'
     codegenJavaPackageName = "com.airbnb.android.react.lottie"
   }
-}
\ No newline at end of file
+}
