diff --git a/gradle/libs.versions.toml b/gradle/libs.versions.toml
index c7502fc..b06c254 100644
--- a/gradle/libs.versions.toml
+++ b/gradle/libs.versions.toml
@@ -1,10 +1,10 @@
 [versions]
-agp = "8.12.0"
+agp = "9.1.0"
 gson = "2.8.9"
 guava = "31.0.1-jre"
 javapoet = "1.13.0"
 junit = "4.13.2"
-kotlin = "2.1.20"
+kotlin = "2.3.20"
 assertj = "3.25.1"
 ktfmt = "0.22.0"
 
diff --git a/react-native-gradle-plugin/build.gradle.kts b/react-native-gradle-plugin/build.gradle.kts
index 85dadeb..2b94884 100644
--- a/react-native-gradle-plugin/build.gradle.kts
+++ b/react-native-gradle-plugin/build.gradle.kts
@@ -64,7 +64,7 @@ kotlin { jvmToolchain(17) }
 
 tasks.withType<KotlinCompile>().configureEach {
   compilerOptions {
-    apiVersion.set(KotlinVersion.KOTLIN_1_8)
+    apiVersion.set(KotlinVersion.KOTLIN_1_9)
     // See comment above on JDK 11 support
     jvmTarget.set(JvmTarget.JVM_11)
     allWarningsAsErrors.set(
diff --git a/react-native-gradle-plugin/src/main/kotlin/com/facebook/react/tasks/GenerateAutolinkingNewArchitecturesFileTask.kt b/react-native-gradle-plugin/src/main/kotlin/com/facebook/react/tasks/GenerateAutolinkingNewArchitecturesFileTask.kt
index 4c36c8a..04bd50a 100644
--- a/react-native-gradle-plugin/src/main/kotlin/com/facebook/react/tasks/GenerateAutolinkingNewArchitecturesFileTask.kt
+++ b/react-native-gradle-plugin/src/main/kotlin/com/facebook/react/tasks/GenerateAutolinkingNewArchitecturesFileTask.kt
@@ -58,40 +58,51 @@ abstract class GenerateAutolinkingNewArchitecturesFileTask : DefaultTask() {
           val cmakeListsPath = dep.cmakeListsPath
           val cxxModuleCMakeListsPath = dep.cxxModuleCMakeListsPath
           if (libraryName != null && cmakeListsPath != null) {
-            // If user provided a custom cmakeListsPath, let's honor it.
             val nativeFolderPath = sanitizeCmakeListsPath(cmakeListsPath)
+            val cmakeListsFilePath = sanitizePath(cmakeListsPath)
             addDirectoryString +=
-                "add_subdirectory(\"$nativeFolderPath\" ${libraryName}_autolinked_build)"
+                """
+                if(EXISTS "$cmakeListsFilePath")
+                  add_subdirectory("$nativeFolderPath" ${libraryName}_autolinked_build)
+                  list(APPEND AUTOLINKED_LIBRARIES $CODEGEN_LIB_PREFIX${libraryName})
+                else()
+                  message(STATUS "Skipping autolinked library ${libraryName}: missing $cmakeListsFilePath")
+                endif()
+                """
+                    .trimIndent()
           }
           if (cxxModuleCMakeListsPath != null) {
-            // If user provided a custom cxxModuleCMakeListsPath, let's honor it.
             val nativeFolderPath = sanitizeCmakeListsPath(cxxModuleCMakeListsPath)
+            val cxxModuleCmakeListsFilePath = sanitizePath(cxxModuleCMakeListsPath)
+            val cxxModuleName = dep.cxxModuleCMakeListsModuleName ?: ""
+            if (addDirectoryString.isNotEmpty()) {
+              addDirectoryString += "\n"
+            }
             addDirectoryString +=
-                "\nadd_subdirectory(\"$nativeFolderPath\" ${libraryName}_cxxmodule_autolinked_build)"
+                """
+                if(EXISTS "$cxxModuleCmakeListsFilePath")
+                  add_subdirectory("$nativeFolderPath" ${libraryName}_cxxmodule_autolinked_build)
+                  if(NOT "$cxxModuleName" STREQUAL "")
+                    list(APPEND AUTOLINKED_LIBRARIES $cxxModuleName)
+                  endif()
+                else()
+                  message(STATUS "Skipping autolinked C++ module ${libraryName}: missing $cxxModuleCmakeListsFilePath")
+                endif()
+                """
+                    .trimIndent()
           }
           addDirectoryString
         }
 
-    val libraryModules =
-        packages.joinToString("\n  ") { dep ->
-          var autolinkedLibraries = ""
-          if (dep.libraryName != null) {
-            autolinkedLibraries += "$CODEGEN_LIB_PREFIX${dep.libraryName}"
-          }
-          if (dep.cxxModuleCMakeListsModuleName != null) {
-            autolinkedLibraries += "\n${dep.cxxModuleCMakeListsModuleName}"
-          }
-          autolinkedLibraries
-        }
-
     return CMAKE_TEMPLATE.replace("{{ libraryIncludes }}", libraryIncludes)
-        .replace("{{ libraryModules }}", libraryModules)
   }
 
   internal fun generateCppFileContent(
       packages: List<ModelAutolinkingDependenciesPlatformAndroidJson>
   ): String {
-    val packagesWithLibraryNames = packages.filter { android -> android.libraryName != null }
+    val packagesWithLibraryNames =
+        packages.filter { android ->
+          android.libraryName != null &&
+              android.cmakeListsPath != null &&
+              File(android.cmakeListsPath).exists()
+        }
 
     val cppIncludes =
         packagesWithLibraryNames.joinToString("\n") { dep ->
@@ -162,6 +173,8 @@ abstract class GenerateAutolinkingNewArchitecturesFileTask : DefaultTask() {
     internal fun sanitizeCmakeListsPath(cmakeListsPath: String): String =
         cmakeListsPath.replace("CMakeLists.txt", "").replace(" ", "\\ ")
 
+    internal fun sanitizePath(path: String): String = path.replace(" ", "\\ ")
+
     // language=cmake
     val CMAKE_TEMPLATE =
         """
@@ -173,11 +186,9 @@ abstract class GenerateAutolinkingNewArchitecturesFileTask : DefaultTask() {
         # or link against a old prefab target (this is needed for React Native 0.76 on).
         set(REACTNATIVE_MERGED_SO true)
 
-        {{ libraryIncludes }}
+        set(AUTOLINKED_LIBRARIES)
 
-        set(AUTOLINKED_LIBRARIES
-          {{ libraryModules }}
-        )
+        {{ libraryIncludes }}
         """
             .trimIndent()
 
diff --git a/react-native-gradle-plugin/src/main/kotlin/com/facebook/react/utils/AgpConfiguratorUtils.kt b/react-native-gradle-plugin/src/main/kotlin/com/facebook/react/utils/AgpConfiguratorUtils.kt
index 2608e77..3dcd8bd 100644
--- a/react-native-gradle-plugin/src/main/kotlin/com/facebook/react/utils/AgpConfiguratorUtils.kt
+++ b/react-native-gradle-plugin/src/main/kotlin/com/facebook/react/utils/AgpConfiguratorUtils.kt
@@ -7,9 +7,8 @@
 
 package com.facebook.react.utils
 
+import com.android.build.api.dsl.LibraryExtension
 import com.android.build.api.variant.ApplicationAndroidComponentsExtension
-import com.android.build.api.variant.LibraryAndroidComponentsExtension
-import com.android.build.gradle.LibraryExtension
 import com.facebook.react.ReactExtension
 import com.facebook.react.utils.ProjectUtils.isEdgeToEdgeEnabled
 import com.facebook.react.utils.ProjectUtils.isHermesEnabled
@@ -81,8 +80,9 @@ internal object AgpConfiguratorUtils {
     appProject.rootProject.allprojects { subproject ->
       subproject.pluginManager.withPlugin("com.android.library") {
         subproject.extensions
-            .getByType(LibraryAndroidComponentsExtension::class.java)
-            .finalizeDsl { ext -> ext.buildFeatures.buildConfig = true }
+            .findByType(LibraryExtension::class.java)
+            ?.buildFeatures
+            ?.buildConfig = true
       }
     }
   }
@@ -114,11 +114,10 @@ internal object AgpConfiguratorUtils {
     appProject.rootProject.allprojects { subproject ->
       subproject.pluginManager.withPlugin("com.android.library") {
         subproject.extensions
-            .getByType(LibraryAndroidComponentsExtension::class.java)
-            .finalizeDsl { ext ->
+            .findByType(LibraryExtension::class.java)
+            ?.let { ext ->
               if (ext.namespace == null) {
-                val android = subproject.extensions.getByType(LibraryExtension::class.java)
-                val manifestFile = android.sourceSets.getByName("main").manifest.srcFile
+                val manifestFile = ext.sourceSets.getByName("main").manifest.srcFile
 
                 manifestFile
                     .takeIf { it.exists() }
diff --git a/react-native-gradle-plugin/src/test/kotlin/com/facebook/react/tasks/GenerateAutolinkingNewArchitecturesFileTaskTest.kt b/react-native-gradle-plugin/src/test/kotlin/com/facebook/react/tasks/GenerateAutolinkingNewArchitecturesFileTaskTest.kt
index 2b3f17d..44575c8 100644
--- a/react-native-gradle-plugin/src/test/kotlin/com/facebook/react/tasks/GenerateAutolinkingNewArchitecturesFileTaskTest.kt
+++ b/react-native-gradle-plugin/src/test/kotlin/com/facebook/react/tasks/GenerateAutolinkingNewArchitecturesFileTaskTest.kt
@@ -128,11 +128,8 @@ class GenerateAutolinkingNewArchitecturesFileTaskTest {
             # or link against a old prefab target (this is needed for React Native 0.76 on).
             set(REACTNATIVE_MERGED_SO true)
 
+            set(AUTOLINKED_LIBRARIES)
 
-
-            set(AUTOLINKED_LIBRARIES
-              
-            )
             """
                 .trimIndent()
         )
@@ -155,15 +152,26 @@ class GenerateAutolinkingNewArchitecturesFileTaskTest {
             # or link against a old prefab target (this is needed for React Native 0.76 on).
             set(REACTNATIVE_MERGED_SO true)
 
-            add_subdirectory("./a/directory/" aPackage_autolinked_build)
-            add_subdirectory("./another/directory/with\ spaces/" anotherPackage_autolinked_build)
-            add_subdirectory("./another/directory/cxx/" anotherPackage_cxxmodule_autolinked_build)
-
-            set(AUTOLINKED_LIBRARIES
-              react_codegen_aPackage
-              react_codegen_anotherPackage
-            another_cxxModule
-            )
+            set(AUTOLINKED_LIBRARIES)
+
+            if(EXISTS "./a/directory/CMakeLists.txt")
+              add_subdirectory("./a/directory/" aPackage_autolinked_build)
+              list(APPEND AUTOLINKED_LIBRARIES react_codegen_aPackage)
+            else()
+              message(STATUS "Skipping autolinked library aPackage: missing ./a/directory/CMakeLists.txt")
+            endif()
+            if(EXISTS "./another/directory/with\ spaces/CMakeLists.txt")
+              add_subdirectory("./another/directory/with\ spaces/" anotherPackage_autolinked_build)
+              list(APPEND AUTOLINKED_LIBRARIES react_codegen_anotherPackage)
+            else()
+              message(STATUS "Skipping autolinked library anotherPackage: missing ./another/directory/with\ spaces/CMakeLists.txt")
+            endif()
+            if(EXISTS "./another/directory/cxx/CMakeLists.txt")
+              add_subdirectory("./another/directory/cxx/" anotherPackage_cxxmodule_autolinked_build)
+              if(NOT "another_cxxModule" STREQUAL "")
+                list(APPEND AUTOLINKED_LIBRARIES another_cxxModule)
+              endif()
+            else()
+              message(STATUS "Skipping autolinked C++ module anotherPackage: missing ./another/directory/cxx/CMakeLists.txt")
+            endif()
             """
                 .trimIndent()
         )
diff --git a/settings-plugin/build.gradle.kts b/settings-plugin/build.gradle.kts
index 2fbeafd..49b002f 100644
--- a/settings-plugin/build.gradle.kts
+++ b/settings-plugin/build.gradle.kts
@@ -54,7 +54,7 @@ kotlin { jvmToolchain(17) }
 
 tasks.withType<KotlinCompile>().configureEach {
   compilerOptions {
-    apiVersion.set(KotlinVersion.KOTLIN_1_8)
+    apiVersion.set(KotlinVersion.KOTLIN_1_9)
     // See comment above on JDK 11 support
     jvmTarget.set(JvmTarget.JVM_11)
     allWarningsAsErrors.set(
diff --git a/shared/build.gradle.kts b/shared/build.gradle.kts
index 3159848..599806e 100644
--- a/shared/build.gradle.kts
+++ b/shared/build.gradle.kts
@@ -33,7 +33,7 @@ kotlin { jvmToolchain(17) }
 
 tasks.withType<KotlinCompile>().configureEach {
   compilerOptions {
-    apiVersion.set(KotlinVersion.KOTLIN_1_8)
+    apiVersion.set(KotlinVersion.KOTLIN_1_9)
     // See comment above on JDK 11 support
     jvmTarget.set(JvmTarget.JVM_11)
     allWarningsAsErrors.set(
diff --git a/shared-testutil/build.gradle.kts b/shared-testutil/build.gradle.kts
index 26173fb..dfea5ff 100644
--- a/shared-testutil/build.gradle.kts
+++ b/shared-testutil/build.gradle.kts
@@ -27,7 +27,7 @@ kotlin { jvmToolchain(17) }
 
 tasks.withType<KotlinCompile>().configureEach {
   compilerOptions {
-    apiVersion.set(KotlinVersion.KOTLIN_1_8)
+    apiVersion.set(KotlinVersion.KOTLIN_1_9)
     // See comment above on JDK 11 support
     jvmTarget.set(JvmTarget.JVM_11)
     allWarningsAsErrors.set(
