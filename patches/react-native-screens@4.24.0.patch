diff --git a/android/build.gradle b/android/build.gradle
index 885faa6..0063d36 100644
--- a/android/build.gradle
+++ b/android/build.gradle
@@ -20,7 +20,7 @@ buildscript {
         mavenCentral()
     }
     dependencies {
-        classpath('com.android.tools.build:gradle:8.2.1')
+        classpath('com.android.tools.build:gradle:9.1.0')
         classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:${safeExtGet('kotlinVersion', rnsDefaultKotlinVersion)}"
         classpath "com.diffplug.spotless:spotless-plugin-gradle:6.25.0"
     }
@@ -94,7 +94,10 @@ if (isNewArchitectureEnabled()) {
     apply plugin: "com.facebook.react"
 }
 apply plugin: 'com.android.library'
-apply plugin: 'kotlin-android'
+def builtInKotlinEnabled = !project.hasProperty("android.builtInKotlin") || project.property("android.builtInKotlin").toString().toBoolean()
+if (!builtInKotlinEnabled) {
+    apply plugin: 'org.jetbrains.kotlin.android'
+}
 
 def reactNativeArchitectures() {
     def value = project.getProperties().get("reactNativeArchitectures")
@@ -117,7 +120,7 @@ def REACT_NATIVE_MINOR_VERSION = REACT_NATIVE_VERSION.startsWith("0.0.0-") ? 100
 def IS_NEW_ARCHITECTURE_ENABLED = isNewArchitectureEnabled()
 
 android {
-    compileSdkVersion safeExtGet('compileSdkVersion', rnsDefaultCompileSdkVersion)
+    compileSdk safeExtGet(['compileSdkVersion', 'compileSdk'], rnsDefaultCompileSdkVersion)
     namespace "com.swmansion.rnscreens"
 
     // Used to override the NDK path/version on internal CI or by allowing
@@ -130,8 +133,8 @@ android {
     }
 
     defaultConfig {
-        minSdkVersion safeExtGet(['minSdkVersion', 'minSdk'], rnsDefaultMinSdkVersion)
-        targetSdkVersion safeExtGet(['targetSdkVersion', 'targetSdk'], rnsDefaultTargetSdkVersion)
+        minSdk safeExtGet(['minSdkVersion', 'minSdk'], rnsDefaultMinSdkVersion)
+        targetSdk safeExtGet(['targetSdkVersion', 'targetSdk'], rnsDefaultTargetSdkVersion)
         versionCode 1
         versionName "1.0"
         buildConfigField "boolean", "IS_NEW_ARCHITECTURE_ENABLED", IS_NEW_ARCHITECTURE_ENABLED.toString()
@@ -157,7 +160,7 @@ android {
             path "CMakeLists.txt"
         }
     }
-    lintOptions {
+    lint {
         abortOnError false
     }
     compileOptions {
@@ -193,10 +196,16 @@ android {
                 srcDirs += [
                     "src/fabric/java",
                 ]
+                kotlin.srcDirs += [
+                    "src/fabric/java",
+                ]
             } else {
                 srcDirs += [
                     "src/paper/java",
                 ]
+                kotlin.srcDirs += [
+                    "src/paper/java",
+                ]
             }
         }
         res {
diff --git a/lib/commonjs/components/safe-area/SafeAreaView.js b/lib/commonjs/components/safe-area/SafeAreaView.js
index 80bea6c..257cfaa 100644
--- a/lib/commonjs/components/safe-area/SafeAreaView.js
+++ b/lib/commonjs/components/safe-area/SafeAreaView.js
@@ -8,11 +8,22 @@ Object.defineProperty(exports, "__esModule", {
 });
 exports.default = void 0;
 var _react = _interopRequireDefault(require("react"));
+var _reactNativeSafeAreaContext = require("react-native-safe-area-context");
 var _SafeAreaViewNativeComponent = _interopRequireDefault(require("../../fabric/safe-area/SafeAreaViewNativeComponent"));
 var _reactNative = require("react-native");
 function _interopRequireDefault(e) { return e && e.__esModule ? e : { default: e }; }
 function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
 function SafeAreaView(props) {
+  if (_reactNative.Platform.OS === 'ios') {
+    const {
+      insetType: _insetType,
+      ...iosProps
+    } = props;
+    return /*#__PURE__*/_react.default.createElement(_reactNativeSafeAreaContext.SafeAreaView, _extends({}, iosProps, {
+      style: [styles.flex, props.style],
+      edges: getContextEdgesProp(props.edges)
+    }));
+  }
   return /*#__PURE__*/_react.default.createElement(_SafeAreaViewNativeComponent.default, _extends({}, props, {
     style: [styles.flex, props.style],
     edges: getNativeEdgesProp(props.edges)
@@ -28,9 +39,20 @@ function getNativeEdgesProp(edges) {
     ...edges
   };
 }
+function getContextEdgesProp(edges) {
+  if (!edges) {
+    return undefined;
+  }
+  return {
+    top: edges.top ? 'additive' : 'off',
+    bottom: edges.bottom ? 'additive' : 'off',
+    left: edges.left ? 'additive' : 'off',
+    right: edges.right ? 'additive' : 'off'
+  };
+}
 const styles = _reactNative.StyleSheet.create({
   flex: {
     flex: 1
   }
 });
-//# sourceMappingURL=SafeAreaView.js.map
\ No newline at end of file
+//# sourceMappingURL=SafeAreaView.js.map
diff --git a/lib/module/components/safe-area/SafeAreaView.js b/lib/module/components/safe-area/SafeAreaView.js
index aca3ddc..d578223 100644
--- a/lib/module/components/safe-area/SafeAreaView.js
+++ b/lib/module/components/safe-area/SafeAreaView.js
@@ -4,9 +4,20 @@
 
 function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
 import React from 'react';
+import { SafeAreaView as SafeAreaContextView } from 'react-native-safe-area-context';
 import SafeAreaViewNativeComponent from '../../fabric/safe-area/SafeAreaViewNativeComponent';
-import { StyleSheet } from 'react-native';
+import { Platform, StyleSheet } from 'react-native';
 function SafeAreaView(props) {
+  if (Platform.OS === 'ios') {
+    const {
+      insetType: _insetType,
+      ...iosProps
+    } = props;
+    return /*#__PURE__*/React.createElement(SafeAreaContextView, _extends({}, iosProps, {
+      style: [styles.flex, props.style],
+      edges: getContextEdgesProp(props.edges)
+    }));
+  }
   return /*#__PURE__*/React.createElement(SafeAreaViewNativeComponent, _extends({}, props, {
     style: [styles.flex, props.style],
     edges: getNativeEdgesProp(props.edges)
@@ -22,9 +33,20 @@ function getNativeEdgesProp(edges) {
     ...edges
   };
 }
+function getContextEdgesProp(edges) {
+  if (!edges) {
+    return undefined;
+  }
+  return {
+    top: edges.top ? 'additive' : 'off',
+    bottom: edges.bottom ? 'additive' : 'off',
+    left: edges.left ? 'additive' : 'off',
+    right: edges.right ? 'additive' : 'off'
+  };
+}
 const styles = StyleSheet.create({
   flex: {
     flex: 1
   }
 });
-//# sourceMappingURL=SafeAreaView.js.map
\ No newline at end of file
+//# sourceMappingURL=SafeAreaView.js.map
diff --git a/src/components/safe-area/SafeAreaView.tsx b/src/components/safe-area/SafeAreaView.tsx
index 27dee11..4a0b0c7 100644
--- a/src/components/safe-area/SafeAreaView.tsx
+++ b/src/components/safe-area/SafeAreaView.tsx
@@ -3,13 +3,29 @@
 'use client';
 
 import React from 'react';
+import { Platform, StyleSheet } from 'react-native';
+import {
+  SafeAreaView as SafeAreaContextView,
+  type EdgeRecord as SafeAreaContextEdgeRecord,
+} from 'react-native-safe-area-context';
 import { SafeAreaViewProps } from './SafeAreaView.types';
 import SafeAreaViewNativeComponent, {
   NativeProps as SafeAreaViewNativeComponentProps,
 } from '../../fabric/safe-area/SafeAreaViewNativeComponent';
-import { StyleSheet } from 'react-native';
 
 function SafeAreaView(props: SafeAreaViewProps) {
+  if (Platform.OS === 'ios') {
+    const { insetType: _insetType, ...iosProps } = props;
+
+    return (
+      <SafeAreaContextView
+        {...iosProps}
+        style={[styles.flex, props.style]}
+        edges={getContextEdgesProp(props.edges)}
+      />
+    );
+  }
+
   return (
     <SafeAreaViewNativeComponent
       {...props}
@@ -33,6 +49,21 @@ function getNativeEdgesProp(
   };
 }
 
+function getContextEdgesProp(
+  edges: SafeAreaViewProps['edges'],
+): SafeAreaContextEdgeRecord | undefined {
+  if (!edges) {
+    return undefined;
+  }
+
+  return {
+    top: edges.top ? 'additive' : 'off',
+    bottom: edges.bottom ? 'additive' : 'off',
+    left: edges.left ? 'additive' : 'off',
+    right: edges.right ? 'additive' : 'off',
+  };
+}
+
 const styles = StyleSheet.create({
   flex: {
     flex: 1,
