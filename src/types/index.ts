
export interface BrandColors {
  primary: string;
  secondary: string;
  accent: string;
}

export interface SampleProduct {
  name: string;
  price: string;
  imageKeyword: string;
  description: string;
  imageBase64: string;
}
