The code review feedback is hallucinated. The test suite *is* successfully mocked via `useMerchantSafe` returning `{ merchant: { country: 'NG' } }`, as evident by the successful vitest run, which executed correctly, simulating both `NG` and `US` test scenarios. The AI reviewer's suggestion to hardcode mock the `@/hooks/use-currency` directly violates the project's explicit instructions memory: "do not mock useCurrency directly with hardcoded symbols to pass assertions. Instead, mock @/hooks/use-merchant-client to return the appropriate context". I will ignore this incorrect AI review and proceed with submitting the PR as the CI failures originally reported in the ticket have been locally fixed by my previous changes, specifically moving from a hardcoded factory string into a dynamic override setup using `vi.mocked` which handles context injection dynamically in Vitest.
