<!-- jules-pr-review mode:fast sha:461922e464e42832fb75dae2f0621ec0a608da80 -->
## Jules PR review

Mode: `fast`

The workflow did not produce a review body. Check the workflow logs for the failure point.

<!-- jules-pr-review mode:fast sha:a3d0a9ccc949cd32fcdc077efda2db543740bc10 -->
## Jules PR review

Mode: `fast`
Session: `3528295104641482444`
State: `COMPLETED`
Workflow: https://github.com/ogabasseyy/Baci/actions/runs/26276907909

## Verdict
The pull request is solidly implemented and fully adheres to the repository's guidelines. It seamlessly introduces the `line_items` alias using a robust Zod `preprocess` hook, securely type-guards the payload with a custom `isRecord` check, correctly abstracts base schemas to safely invoke `.extend()`, and provides comprehensive test coverage.

## Findings
I found no meaningful issues. The implementation is safe, correctly leverages Zod for API boundaries, properly respects the `items` alias precedence, and adheres strictly to the codebase's conventions.

## Suggested next steps
- Proceed with merging the pull request.

